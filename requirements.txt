# Aether — install everything needed to run the agent and sidecar.
#   pip install -r requirements.txt -r requirements-sidecar.txt   (dev + tests)
#   pip install -r requirements.lock                             (exact pins)
-r requirements-runtime.txt

# Local fast model: use Ollama (https://ollama.com) — no Python package required.
# Optional local voice engines (runtime-detected): mlx-whisper or pywhispercpp for
# STT, kokoro for TTS. Optional local embeddings: sentence-transformers.
