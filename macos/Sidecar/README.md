# Sidecar (Python)

The HTTP sidecar implementation lives at the repo root: [`../../sidecar/`](../../sidecar/).

```bash
cd ../..   # repo root
python -m sidecar.server
```

See [`../../sidecar/README.md`](../../sidecar/README.md) for API details.
